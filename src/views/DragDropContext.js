import React from 'react';

const DragDropContext = ({children}) => {
    // Puedes manejar cualquier estado o comportamiento compartido aquí.
    return <div>{children}</div>;
};

export default DragDropContext;
