export class SDAction {
    constructor(type, title, message, destination) {
        this.type = type;
        this.title = title;
        this.message = message;
        this.destination = destination;
    }
}
