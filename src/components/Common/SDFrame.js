class SDFrame {
    constructor(height, width, minWidth, maxWidth, minHeight, maxHeight, alignment) {
        this.height = height;
        this.width = width;
        this.minWidth = minWidth;
        this.maxWidth = maxWidth;
        this.minHeight = minHeight;
        this.maxHeight = maxHeight;
        this.alignment = alignment;
    }

}

export default SDFrame;
